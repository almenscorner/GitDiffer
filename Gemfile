# frozen_string_literal: true
source "https://rubygems.org"

gem "jekyll", "~> 4.3"
# Ruby 3+ no longer ships with webrick by default; needed for `jekyll serve`
gem "webrick", "~> 1.8"

group :jekyll_plugins do
  # Add Jekyll plugins here if needed, e.g. feeds or sitemap
  # gem "jekyll-feed", "~> 0.15"
end
